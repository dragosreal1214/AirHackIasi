# Aerly docs

- `decisions/` — Architecture Decision Records (ADRs).
- `api-spec.md` — API contract (truth source). _To be authored alongside the API._
- `ml-validation-report.md` — generated by the ML validation scripts.

The full 48-hour execution plan (tasks, waves, ownership) lives in the team
planning doc. Per-task prompts reference `docs/tasks.md` — drop the task
breakdown there when the team wants it tracked in-repo.
